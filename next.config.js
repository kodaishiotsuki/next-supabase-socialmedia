/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["ipuwsfezhwmneoaefknw.supabase.co", "lh3.googleusercontent.com"],
  },
};

module.exports = nextConfig;
